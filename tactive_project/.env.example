# Flask environment
APP_ENV=development

# Secret key (change in production — never commit the real value)
SECRET_KEY=replace-me-with-a-random-secret-key

# MongoDB
MONGO_URI=mongodb://localhost:27017/
MONGO_DBNAME=datacenter_db
